const cards = {
  primary: {
    padding: 3,
    margin: 0,
    borderWidth: 3,
    borderStyle: `solid`,
    boxShadow: `default`,
    borderRadius: 2,
    borderColor: `primary`,
    transition: `card`,
  },
}

export default cards
